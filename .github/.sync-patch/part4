xt;
-      } else {
-        selectedGlyphIds = new Set([glyph.id]);
-      }
+      } else selectedGlyphIds = new Set([glyph.id]);
       if (selectedGlyphIds.size > 1) refreshMultiSelection(0); else multiSelectionBox = null;
       draw(); syncUI(); return;
     }
@@ -932,18 +1062,15 @@
       for (const s of interaction.glyphSnapshot) {
         const g = doc.glyphs.find(x => x.id === s.id); if (g) { g.x = s.x + dx; g.y = s.y + dy; }
       }
-      if (interaction.mode === 'moveGlyphGroup' && multiSelectionBox) {
-        multiSelectionBox.cx = interaction.startBox.cx + dx; multiSelectionBox.cy = interaction.startBox.cy + dy;
-      }
+      if (interaction.mode === 'moveGlyphGroup' && multiSelectionBox) { multiSelectionBox.cx = interaction.startBox.cx + dx; multiSelectionBox.cy = interaction.startBox.cy + dy; }
       draw(); syncUI(); return;
     }
     if (interaction.mode === 'rotateSingleGlyph' || interaction.mode === 'rotateGlyphGroup') {
       const angle = Math.atan2(p.y - interaction.center.y, p.x - interaction.center.x) - interaction.startAngle;
       for (const old of interaction.glyphSnapshot) {
         const g = doc.glyphs.find(x => x.id === old.id); if (!g) continue;
-        if (interaction.mode === 'rotateSingleGlyph') {
-          g.rotation = old.rotation + angle * 180 / Math.PI;
-        } else {
+        if (interaction.mode === 'rotateSingleGlyph') g.rotation = old.rotation + angle * 180 / Math.PI;
+        else {
           const q = Core.rotatePoint(old.x, old.y, interaction.center.x, interaction.center.y, angle);
           g.x = q.x; g.y = q.y; g.rotation = old.rotation + angle * 180 / Math.PI;
         }
@@ -994,6 +1121,7 @@
   els.noiseAmountText.textContent = '0%';
   els.noiseSizeText.textContent = '1px';
   els.noiseColorText.textContent = '关闭';
+  rebuildPaperMap();
   syncUI();
   draw();
   loadPreset('yunyan');
diff --git a/standalone.html b/standalone.html
index 9feae51..99218eb 100644
--- a/standalone.html
+++ b/standalone.html
@@ -12,7 +12,7 @@
     <div>
       <div class="eyebrow">HANDWRITER · MVP+</div>
       <h1>HandWriter Editor</h1>
-      <p>文本框首轮排版，确认后解除编组；支持单字编辑、临时多选编组、背景与噪点、局部墨色不均匀。</p>
+      <p>文本框首轮排版，确认后解除编组；支持单字编辑、临时多选编组、背景融合、纸纹采样、局部压印/渗墨、局部墨色不均匀。</p>
     </div>
     <div class="fontbar">
       <select id="presetSelect" class="fontselect" aria-label="字体预设">
@@ -62,8 +62,8 @@
         </div>
         <div class="split3">
           <div class="field"><label>文字融合强度 <span class="sub" id="noiseAmountText">0%</span></label><input id="noiseAmount" type="range" min="0" max="1" step="0.05" value="0"></div>
-          <div class="field"><label>噪点尺寸 <span class="sub" id="noiseSizeText">1px</span></label><input id="noiseSize" type="range" min="1" max="4" step="1" value="1"></div>
-          <div class="field"><label>彩色噪点 <span class="sub" id="noiseColorText">关闭</span></label><input id="noiseColor" type="range" min="0" max="1" step="1" value="0"></div>
+          <div class="field"><label>渗墨/毛边范围 <span class="sub" id="noiseSizeText">1px</span></label><input id="noiseSize" type="range" min="1" max="4" step="1" value="1"></div>
+          <div class="field"><label>彩色纸面偏色 <span class="sub" id="noiseColorText">关闭</span></label><input id="noiseColor" type="range" min="0" max="1" step="1" value="0"></div>
         </div>
         <div class="split2">
           <button id="newTextboxBtn" class="primary">新建文本框</button>
